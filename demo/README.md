# Docket demo plan

The two plans compare captions across identical scenes and hold times. The shorter candidate is the first recording choice. Planned duration and reading rate are not actual video measurements. Use a real browser recording, preserve network waits and errors, and label any cut in time if needed. Never substitute a saved successful check for a failed live call without saying it is saved evidence.

Start the app, select the clearly labelled public transaction example, and show its invoice expectation. Expand the transaction fingerprint, run a real source check, then verify inclusion. Save an explanatory review note and show the public/private export distinction. Public registry status must match the actual release state. If a receipt lookup fails, show the unavailable state and retry or retain the failed take.

The external native tamper test is useful supporting evidence; do not imply the UI has a tamper-testing button. The contract's unit tests use a named test verifier; they do not replace live sponsor proof.

Capture captions in the finished video, use no narration or music, check full decoding and playback, and inspect both small text and visible results. Confirm the actual event duration rule again before final submission. Keep the raw take, caption file and measured recording metadata. No recorded video exists merely because these plans exist.

## Actual recording, 6 September 2026

The public browser workspace at https://gylshaurya.github.io/docket/ was recorded through real tab screenshots while taking actual UI actions. The recording is 130.3 seconds, 1280 by 816, H.264 at 30 output frames per second with 4.54 captured source frames per second. Captions occupy the separate lower strip. No narration or generated product footage is used. Only idle gaps between scenes were cut; no action was sped up or failed action removed.

The source transaction and illustrative invoice are clearly labelled. Actual receipt, exact ETH match, CC3 inclusion, saved reviewer note and reload persistence passed during recording. The registry gas blocker is shown. Public and private export choices appear separately. This is a demo of the current release, not a claim that registry deployment is complete.

The planned comparison files remain planned evidence. Actual measurements are in recording.json and captions.srt. The public release is https://github.com/gylshaurya/docket/releases/tag/demo-v1.
